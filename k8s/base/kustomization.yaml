apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: allen-cahn-cuda-base

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/version: "1.0.0"

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - job.yaml
